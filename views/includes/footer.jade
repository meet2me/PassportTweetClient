script(type='text/javascript').
      $( document ).ready(function() {
        var plot = $.plot($("#placeholder"), [ [[0, 0], [1, 1]] ], { yaxis: { max: 1 } });
        var plot = $("#placeholder").plot([ [[0, 0], [1, 1]] ], { yaxis: { max: 1 } } ).data("plot");
      });